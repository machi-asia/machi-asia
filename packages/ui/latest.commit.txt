feature(footer-nav-align): wide Footer component + Navbar linkAlign control
- footer: new src/lib/Footer (Footer.tsx, Footer.module.css, index.ts, Footer.test.tsx) — full-width band with 1200px inner container; optional `brand` slot accepts any node (text or image/logo markup) rendered on the left, `links` row of {label, href} anchors pushed right via margin-left:auto and wrapped as chips under 640px, free-form children between brand and links (.content flexes for e.g. newsletter forms), separated bottom `note` bar with border-top for copyright, variants solid/transparent; forwardRef + HTMLAttributes spread like the rest of the library
- navbar: new `linkAlign?: 'left' | 'center' | 'right'` prop (default 'left', exported type NavbarLinkAlign) — alignment classes apply justify-content to only the .links group inside its flexible space, so brand stays pinned left and actions/sign-in stay pinned right regardless of setting; mobile drawer unaffected
- navbar: brand slot confirmed/locked in for images — .brand now caps img/svg at max-height:36px width:auto (mirroring Footer's guard) so logo files can't overflow the 64px bar; regression test renders an <img>+text brand node; playground "Center" alignment demo uses an inline SVG logo mark to show it visually
- barrels: Footer/FooterLink/FooterProps/FooterVariant exported from root index.ts; NavbarLinkAlign added to Navbar/index.ts and root
- tests: 5 Footer tests (landmark+links, optional brand render/omission, note row, transparent variant + children without link list), 3 Navbar alignment tests (default left, center keeps brand first/actions last in DOM, right), and 1 Navbar image-brand test; suite now 164/164 across 24 files (one Modal Escape test flaked once under parallel workers — passes standalone and in full re-run, unrelated to these changes)
- playground: new /footer page (solid with text brand + copyright note, inline-SVG-logo brand with two-part note, transparent variant, notes list) registered in layout NAV and home LINKS; /navbar page gained a "Link alignment" section with center/right demo navbars
- README: Footer table row added; Navbar row updated for linkAlign + image/logo brand support
- verification: npm run typecheck clean, 164/164 vitest pass, vite lib build succeeds, npx tsc -p playground/tsconfig.json exit 0
- pending from earlier sessions (still uncommitted): fix(navbar-drawer) portal/ref-counted scroll-lock/useId drawer, feature(navbar-auth) primary variant + AuthModal + OAuth icons, feature(navbar-links) href/targetId/children nav item types, chore(package) @machi/ui -> @machi-asia/ui rename across package.json/package-lock/playground
