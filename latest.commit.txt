feature(rose): add shippable RoseChatModal compound component with shared triggers

- packages/rose: add RoseChatModal compound component (RoseChatModalProvider, RoseChatModal full-screen overlay, RoseChatModalActionButton and RoseChatModalFloatingButton triggers); every trigger opens the same modal instance via a shared context so there is never a duplicate modal; passing-through RoseChat props make sidebar and header visibility configurable
- packages/rose: export RoseChatModal and its trigger/provider components + props types from the package index; add full-screen overlay, close button, FAB, and action-button styles to the shared styles.css (consumed via @machi-asia/rose/styles.css in the host layout)
- README.md: document the new RoseChatModal compound component in the rose component list

- apps/test: add dynamic enumerable-prop dropdowns to the explorer — a new lib/analyzer.ts parses each package's TypeScript types (collecting literal-union aliases across .ts/.tsx files, resolving inline unions, named/cross-file aliases like AuthMode, array-of-union props like toolbar, and numeric unions like weekStartsOn) and surfaces a dropdown for every enumerable prop on any component in any package (UI and Rose alike: Button variant/size, Card variant/padding, Accordion/Dropdown/Navbar/Footer/Gallery/Table/Tabs/Switch/Toast/TextEditor variants, Modal size, ScrollReveal variant, ScrollCurtain/ScrollSplit direction, ScrollZoom mode, AuthModal initialMode, Calendar weekStartsOn, rose Chatbot component variant 'main'/'compact'/'chatbot', and MarkdownRenderer 'main'/'chatbot'/'compact'); boolean props now surface as true/false toggles (RoseChat requireAuth/showSidebar/showHeader/showMemories/showUsage/showLogout, Modal open, Button loading/fullWidth, Navbar sticky, etc.); the scanner attaches enumerableProps to each ComponentFile, the RoseChat preview renders the full component so its layout switches live-toggle, and the hand-maintained lib/props.ts registry is removed
- apps/test: fix scanner classification so framework/file noise is excluded — Next app-router files (layout.tsx anywhere and app/page.tsx root pages) are removed from the scan entirely, test/spec files (names matching .test./.spec. and __tests__ dirs) are excluded instead of listed as "Test Suite", and remaining framework-named showcase pages (playground/app/*/page.tsx) are surfaced by their parent folder name (accordion/button/calendar/...) instead of "page"; the explorer and its counts no longer include layouts, root pages, or test files
- apps/test: mount the shared API route handlers (/api/auth/*, /api/health, /api/usage, /api/rose/* including sessions/[id]/chat and memories/[id], /api/media-library/media*) behind a gateway-style middleware.ts that verifies bearer tokens and injects x-gateway-sub/x-gateway-roles, so API-backed component previews work in the explorer; expand .env.example with Supabase/Gemini/usage-limit vars
- apps/rose, apps/test, apps/tween: add tsconfig.typecheck.json (excludes .next) and point the typecheck script at it so tsc never races with next build over .next/types in the combined turbo pipeline
- docs: ship component-docs.json manifests + COMPONENTS.md for every package (ui, auth, api-gateway, media-library, rose) documenting description, requiresAuth, requiresProviders, requiresEnv, apiEndpoints, dependencies, usage; scanner loads manifests and the explorer renders a Docs & Requirements tab
- packages/rose: add long-term memory system — rose_memories migration, MemoriesSettingsModal, memories routes (GET/POST, PATCH/DELETE), takeNotes tool, memoryStore helpers; remove ChatInterface.tsx in favor of RoseChat/Chat
- packages/rose: add roseEnv helpers (roseApiBase/roseApiUrl/roseGatewayUrl/isBrowserSupabaseConfigured); RoseChat/UsageBar/MemoriesSettingsModal accept apiBasePath with gateway-aware default; getBrowserSupabase returns null when env unset; Providers takes authApiUrl prop with same-origin default
- packages/api-gateway: UsageCard reads the token from machi_access_token (fallback access_token) instead of the never-written access_token key, accepts apiBasePath, and guards Realtime when Supabase env is missing
- packages/media-library: MediaLibraryModal/MediaPage accept apiBasePath (mediaApiBase helper), Providers takes authApiUrl prop with same-origin default, Realtime guards on env presence
- db: apply rose_memories and media table migrations to the live Supabase project, add authenticated RLS SELECT policies for usage_limits/rose_sessions/rose_session_messages/media, and publish those tables (plus rose_memories) to supabase_realtime (saved as timestamped migration files per package)
- README.md: document the test app API mounting + middleware, the per-package component docs system, and the gateway-aware API base resolution for shipping components

- build(monorepo): fix dev watch pipeline so editing a package recompiles it and all downstream apps — @machi-asia/ui previously drove its dev via "next dev playground" (a no-op for building the library), so editing UI source never rebuilt dist/ and consuming apps never recompiled; the ui dev script now runs vite build --watch plus tsc -p tsconfig.build.json --watch together (via the new concurrently devDependency) so both the JS bundle and the .d.ts declarations regenerate on every source change and the dev graph (persistent, cache:false) picks the new dist/ up live in every next dev server; the other packages already had tsc --watch dev scripts; turbo.json dev task now declares dependsOn:["^build"] (build is non-persistent, unlike the forbidden persistent ^dev) so each app's dev server only starts after its dependent packages are built — eliminating the transient "Cannot find module '@machi-asia/ui'" / missing .d.ts type errors on startup; README.md documents the watch pipeline
